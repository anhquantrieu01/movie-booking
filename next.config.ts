import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["res.cloudinary.com", "lh3.googleusercontent.com", "i.pravatar.cc", "images.unsplash.com"],
  },
};

export default nextConfig;
